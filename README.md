# UPnP
The example program is used to construct an AddPort Mapping on the server side of the Intranet. The testsocket function listens to the TCP port, waits for the client to send the message, and then prints the message.
